import { View, Text } from 'react-native';
import styles from './RealTime.style'

const RealTime = () => {
  return (
   <View style={styles.container}>
      <Text style={styles.text}>RealTime Screen</Text>
    </View>
  );
};

export default RealTime;
